import React from "react";
import {useState} from 'react'
import { useHistory } from "react-router-dom";

const Button = ({itemToDelete}) => {    
 
    return (
        <button>Plus d'info</button>
    );
}

export default Button;